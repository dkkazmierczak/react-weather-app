import React from "react"

const City = props => {
  return <span id="city">{props.city}</span>
  //document.querySelector("title").innerHTML = `Weather forecast | {response.data.name}`;
}

export default City
